FROM golang:1.26-bookworm

RUN apt-get update \
    && apt-get install -y --no-install-recommends \
      desktop-file-utils \
      dpkg-dev \
      lintian \
      libglib2.0-bin \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /src
CMD ["./packaging/ubuntu/build-deb.sh"]